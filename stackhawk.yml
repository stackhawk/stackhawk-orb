app:
  applicationId: ${APP_ID}
  env: Development
  host: ${HOST}

hawk:
  spider:
    base: false

tags:
  - name: _STACKHAWK_GIT_COMMIT_SHA
    value: ${CIRCLE_SHA1:}
  - name: _STACKHAWK_GIT_BRANCH
    value: ${CIRCLE_BRANCH:}
